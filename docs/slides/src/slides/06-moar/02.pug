h2 Data Classes

p Auto-generate equals, hashCode, toString, copy constructor, and destructuring support

pre.fragment
  code.lang-kotlin
    | // Declare a Data class
    | data class MyDTO(
    |         val alpha: Long,
    |         val beta: Long)

pre.fragment
  code.lang-kotlin
    | val dto = MyDTO(1, 2)
    |
    .fragment
      |
      | println( dto.toString() )     // MyDTO(alpha=1, beta=2)
      |
    .fragment
      |
      | val copy: MyDTO = dto.copy()  // Build duplicate of 'dto'
    .fragment
      |
      | println( dto === copy )       // false, not referentially equal
      |
    .fragment
      |
      | println( dto == copy )        // true, using generated 'equals' method
      |
    .fragment
      |
      | val (shazam, kapowza) = dto   // Destructuring (not quite as nice as ES2016)
